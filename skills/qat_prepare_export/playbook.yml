id: sima-qat-prepare-export
name: Prepare, Export, and Compile QAT Models for SiMa
version: 0.2.0
agents: [codex, claude]
description: Skill for shared-environment FX QAT, Q/DQ ONNX export, Model Compiler acceptance compilation, and MPK artifact handling.
compatibility:
  env_types: [host, sdk]
  env_subtypes: [linux, palette]
  min_cli_version: 2.1.2
runtime:
  requires:
    - python

id: sima-qat-prepare-export
name: Prepare, Export, and Compile QAT Models for SiMa
version: 0.2.3
agents: [codex, claude]
description: >-
  Skill for shared-environment FX QAT, canonical data/build/dist paths, Q/DQ
  ONNX export, Model Compiler acceptance compilation, and MPK artifact handling.
compatibility:
  env_types: [host, sdk]
  env_subtypes: [linux, palette]
  min_cli_version: 2.1.3
runtime:
  requires:
    - python
